program KeyGenerator;

uses
  Forms,
  Form_Main in 'Form_Main.pas' {frm_MainWindow};

{$R *.RES}

begin
  Application.Initialize;
  Application.Title := 'P.U.T RockLog Key Generator';
  Application.CreateForm(Tfrm_MainWindow, frm_MainWindow);
  Application.Run;
end.
